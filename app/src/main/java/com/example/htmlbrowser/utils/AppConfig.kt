package com.example.htmlbrowser.utils

object AppConfig {
    // Note: File size limit has been removed per user request
    const val ARCHIVE_DIR_NAME = "archives"
    const val DATABASE_NAME = "html_archive.db"
    
    val SUPPORTED_FILE_TYPES = arrayOf("application/zip", "application/x-zip-compressed")
}